# Lookout

Lookout is the planned LLM-assisted triage service.

Responsibilities:

- receive bounded alert or case bundles
- add context from Prometheus when needed
- produce a short incident summary
- suggest next checks

Lookout is disabled by default and never performs enforcement.
